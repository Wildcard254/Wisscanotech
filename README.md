# WISSCANO TECHNOLOGIES — Single-file site

This repository has been trimmed to publish a single static page via Cloudflare Pages / Workers.

Files kept at the tip of main:
- `index.html` — site entry (created from `online_viewer_net[1].html`)
- `wrangler.toml` — Cloudflare Workers configuration (intentionally preserved)

Other files were overwritten with small placeholders on 2026-06-09 to leave this repository focused on the single HTML site. The original contents remain available in the git history and can be recovered if needed.

To restore a file from history locally, use:

  git fetch origin
  git checkout <commit-sha> -- <path>

If you want a full history rewrite to permanently remove any files, let me know and I can provide step-by-step instructions to do that locally (it requires force-pushing).

Committed by GitHub Copilot for Wildcard254 on 2026-06-09.
